name: layout_base
description: Base Flutter com layout visual neutro.
publish_to: none
# Versao visivel + numero interno usados tambem pelo Android.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  audioplayers: ^6.7.1
  flutter:
    sdk: flutter
  http: ^1.6.0
  mobile_scanner: ^7.4.0

flutter:
  uses-material-design: true
  assets:
    - lib/frontend/recursos/logo.png
    - lib/frontend/recursos/sons/
